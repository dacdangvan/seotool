# GA4 Worker Environment Variables
# ================================
# This worker supports per-project GA4 configuration stored in database.
# The env vars below are only used for fallback/testing.

# Database Connection (REQUIRED)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_seo_tool
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ai_seo_tool
DB_USER=postgres
DB_PASSWORD=postgres

# Sync Settings
SYNC_DAYS=30
SYNC_INTERVAL_HOURS=6

# ================================
# LEGACY/FALLBACK CONFIG (Optional)
# ================================
# These are only used if you want to sync a single project via env vars
# instead of using the per-project database configuration.
# 
# GA4_PROPERTY_ID=123456789
# GA4_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
# GA4_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# GA4_KEY_FILE_PATH=/path/to/service-account-key.json
# PROJECT_ID=a1b2c3d4-e5f6-7890-abcd-ef1234567890

